# 🐙 Git 使用说明

Git 是一个分布式版本控制系统，用于代码的版本管理和协作开发。本文档总结了 Git 的基础知识与常用命令，帮助快速上手与日常使用。

---

## 📌 基础概念

- **仓库 (Repository)**：保存代码与历史记录的目录。
- **工作区 (Working Directory)**：你实际编写和修改代码的地方。
- **暂存区 (Staging Area)**：准备提交到仓库的更改集合。
- **提交 (Commit)**：保存到仓库的一次快照。
- **分支 (Branch)**：独立的开发线，常见 `main` 或 `master` 为主分支。
- **远程仓库 (Remote)**：如 GitHub、GitLab 上的仓库，用于协作。

---

## ⚙️ 常用配置

第一次使用 Git 时需要配置用户名和邮箱（和提交记录关联）：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

查看配置：
```bash
git config --list
```

## 📂 基础操作流程

初始化仓库
```bash
git init
```

或者克隆远程仓库：
```bash
git clone <仓库地址>
```

查看状态
```bash
git status
```

添加文件到暂存区
```bash
git add <文件名>      # 添加指定文件
git add .             # 添加当前目录所有文件
```

提交更改
```bash
git commit -m "提交说明"
```

查看提交历史
```bash
git log --oneline --graph --all
```

## 🌿 分支操作
创建分支
```bash
git branch <分支名>
```

切换分支
```bash
git checkout <分支名>
# 或更推荐
git switch <分支名>
```

创建并切换分支
```bash
git checkout -b <分支名>
# 或
git switch -c <分支名>
```

合并分支
```bash
git checkout main
git merge <分支名>
```

删除分支
```bash
git branch -d <分支名>
```

## 🌍 远程操作

查看远程仓库
```bash
复制代码
git remote -v
```

添加远程仓库
```bash
git remote add origin <仓库地址>
```

推送代码
```bash
git push origin <分支名>
```

拉取更新
```bash
git pull origin <分支名>
```

获取远程分支但不合并
```bash
git fetch
```

## 🔧 常用技巧
撤销修改（未 add 前）
```bash
git checkout -- <文件名>
# 或 Git 2.23+
git restore <文件名>
```

撤销暂存（已 add 但未 commit）
```bash
git rm --cached <文件名>
```

修改最后一次提交信息

```bash
git commit --amend -m "新的提交说明"
```

回退到某个提交
```bash
git log --oneline # 查看所有提交 （由新到旧）
git reset --hard <commit_id> # 注意，--hard不会保留文件的修改
git reset --soft <commit_id> # 在需要保留文件修改的情况下，使用 --soft
```

## 🚀 常见工作流
### 1. 克隆项目
```bash
git clone https://github.com/user/repo.git
cd repo
```

### 2.新建功能分支并开发
```bash
git switch -c feature-x
```

### 3. 开发完成 → 添加并提交
```bash
git add .
git commit -m "完成 feature-x"
```

### 4. 推送到远程
```bash
git push origin feature-x
```

需要合并`feature-x`分支到`develop`
```bash
# 首先提交feature上的最新更新
git add <文件名或文件夹>
git commit -m "完成 feature-x"
# 有两种更新方式： 
# 方式1. 直接本地合并到develop，并直接提交 （适用于需要紧急修改develop上的代码，且需要直接提交到develop的权限，在远程仓库设置中确认）
git switch devevlop
git merge feature-x # 如有文件冲突，需要在本地解决冲突后再提交
git push

# 方式2. 将本地feature-x上的变动推送至远程后，在远程仓库中建立Merge Request并且选择至少一名Reviewer （适用于大多数情况）
git push
# 然后在远程仓库中操作
在 GitHub/Gitea 上发起 Pull Request / Merge Request，合并到主分支。
```

📖 参考资料

[Git 官方文档](https://git-scm.com/book/zh/v2?utm_source=chatgpt.com)
